import { motion } from 'framer-motion'
import styled from 'styled-components/macro'

export const Container = styled.div`
  position: relative;
  height: 100%;
  overflow: hidden;
`

export const RouteContainer = styled(motion.div)`
  height: 100%;
`
